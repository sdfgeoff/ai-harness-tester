Create an interactive website about 3D printers using npm, react, typescript and vite. When done, build it so there is a `dist` folder containing `index.html`
It should include at minimum information on how they work, turning them, and some suggested projects you can do with a 3D printer.
